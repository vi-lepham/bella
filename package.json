{
  "name": "bella",
  "version": "1.0.0",
  "repository": "https://github.com/vi-lepham/bella.git",
  "author": "Pham Le Vi <vi@monotas.net>",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-minify --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "dependencies": {
    "@barba/core": "^2.9.7",
    "gsap": "^3.6.1",
    "imagesloaded": "^4.1.4",
    "parcel-bundler": "^1.12.5",
    "smooth-scrollbar": "^8.6.2"
  }
}
